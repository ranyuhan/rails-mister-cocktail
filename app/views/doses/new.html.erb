<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, collection: Ingredient.all, hint: "Pick a ingredient from all database"%>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
